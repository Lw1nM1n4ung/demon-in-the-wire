# Wire_Ghost — Portal (nginx + static assets)
# Bakes web/ and nginx.conf into the image so the portal is fully
# rebuildable from source (no bind-mount dependency for source files).
FROM nginx:alpine
COPY web/ /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/conf.d/default.conf
