# Wire_Ghost — Portal (nginx + static assets)
# Hardcoded for 127.0.0.1:2006 — no templates or entrypoint needed.
FROM nginx:alpine
COPY web/ /usr/share/nginx/html/
COPY config/nginx.conf /etc/nginx/conf.d/default.conf
