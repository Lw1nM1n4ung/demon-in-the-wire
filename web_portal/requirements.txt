django>=5.0,<7.0
djangorestframework>=3.15
django-cors-headers>=4.3
mysqlclient>=2.2
pymysql>=1.1
cryptography>=42.0
gunicorn>=22.0
celery[redis]>=5.4
uuid_utils>=0.9
django-celery-beat>=2.6
redis>=5.0
python-docx>=1.1
openpyxl>=3.1
jinja2>=3.1
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.1
pyyaml>=6.0
# Runtime deps of the wireghost scanner package — Celery worker imports
# scanner.tasks → wireghost.pipeline → rich / aiohttp / defusedxml. These
# are declared in pyproject.toml but we don't `pip install -e .` the
# package in-container, so list them explicitly.
rich>=13.0
aiohttp>=3.9
defusedxml>=0.7
# Real-time System Monitor page (cgroup-scoped CPU/mem/disk/net)
psutil>=6.0
